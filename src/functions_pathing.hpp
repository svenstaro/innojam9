#ifndef FUNCTIONS_PATHING_HPP
#define FUNCTIONS_PATHING_HPP

glm::vec2 linear_path(glm::direction, float speed)
{
    direction.x = speed *dt;
    return direction;         
}

glm::vec2 sin_path(glm::direction, float speed)
{
    return glm::vec2(0,0);
}



#endif
